#include <stdio.h>

/**
 * main- program starting point
 * Return: zero of no error
 *
 */

int main(void)

{
	char i;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
